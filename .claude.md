# Claude Instructions (Token Efficient)

- Be concise · bullets > prose · code > explanations
- No restating input · no preamble · minimal tokens
- Do not explain unless asked
- No commit without explicit request

# Project Context Pointer

**Read `contexto.md` first** for full project snapshot (estado por épica, BD, convenciones, pendientes).

## Quick facts
- **Stack:** .NET 8 · WPF · EF Core · Supabase PG (pooler 6543) · MVVM Toolkit · BCrypt
- **Arquitectura:** Clean Architecture 4 capas (`Domain`/`Application`/`Infrastructure`/`Presentation`)
- **Estado actual:** Épicas 1–3 cerradas; KAN-14 Fase 6 (infra/RBAC) en curso; rama `feature/KAN-11-rbac-sesion`
- **Próximo:** Épica 4 (Tasa Retención, KAN-13..16 originales)

## Convenciones críticas
- Interfaces: `IRepositorio*`, `IServicio*`. Use cases: una clase por caso, método `EjecutarAsync`
- Permisos en runtime: **siempre** `SesionActual.TienePermiso("MOD.ACCION")` — nunca BD per-op
- Códigos reales: `US.{CREAR,EDITAR,ELIMINAR,VER}`, `INF.{VER,EDITAR}`, `AUD.VER`
- Auditoría: fire-and-forget vía `IAuditoriaServicio.RegistrarAsync` (no bloquea ni revierte)
- Transacciones: `IUnidadTrabajo.{IniciarTransaccionAsync,ConfirmarTransaccionAsync,RevertirTransaccionAsync}`
- Soft delete: `esta_activo=false` + `eliminado_en` + `eliminado_por_usuario_id`. Hard delete solo si `Estado=Inactivo`
- DbContext: `Scoped`. ViewModels async crean scope manual: `using var scope = _serviceProvider.CreateScope()`
- Sin N+1: batch con `WHERE col = ANY(@ids)`

## Diagramas
- ER en `src/Application/UseCases/BD ER/` (BD-01..BD-03B)
- Clases dominio en `src/Application/UseCases/Diagramas de Clase Dominio/` (DC-01 split en 4, DC-02 split en 2, DC-02.1 corregido)
- Secuencia en `src/Application/UseCases/Diagramas de Secuencia/` (DS-01 reescrito; DS-02 split 3; DS-03 split 4)
- PlantUML: `skinparam X { ... }` multi-línea (sin `;`); `@startuml <nombre>` siempre

## Pendientes conocidos
- Migrar TEXT→`timestamptz` en ~22 tablas (épicas 4–13)
- Migrar INT→`bool` en `cuadra_balance`, `es_viable` y restantes `esta_activo`
- Crear entidades C# para tablas huérfanas (`categoria_activo`, `activo_fijo`, `activo_diferido`, etc.)

# Restore (referencia rápida Supabase)
- Backup: Custom · Schema=public only · No owner · No privileges
- Restore: Pre-data + Data + Post-data · No owner · No privileges
