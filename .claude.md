# Claude / Codex Project Notes

Guia operativa para trabajar en `SistemaAranceles` sin romper la arquitectura ni los flujos ya cerrados.

# Claude Instructions (Token Efficient)

- Be concise · bullets > prose · code > explanations
- No restating input · no preamble · minimal tokens
- Do not explain unless asked
- _No commit without explicit request_ — solo cuando el usuario lo pide textualmente
- No agregar features/refactors/abstracciones más allá de lo pedido
- No comments en código salvo el "porqué" no obvio
- Reutilizar lo existente antes de crear; antes de borrar/renombrar, grep los referenciantes

## Reglas No Negociables

- No hacer commit, push, reset, checkout destructivo ni borrar ramas sin pedido textual del usuario.
- No incluir `Co-Authored-By` ni tags de coautor.
- No ejecutar cambios de schema directamente contra Supabase salvo autorizacion explicita. Preparar SQL idempotente y esperar confirmacion.
- No crear migraciones si el proyecto ya esta usando scripts SQL manuales para ese KAN.
- No tocar calculos fuera del alcance pedido.
- No inyectar `ContextoAplicacion` directamente en ViewModels.
- Antes de editar: leer el modulo afectado, buscar referencias con `rg`, y respetar patrones locales.
- Usar parches para editar archivos.

## Snapshot Actual

- Rama reciente: `feature/KAN-34-Materiales-Inflacion`.
- Stack: .NET 8, WPF, EF Core, PostgreSQL/Supabase, CommunityToolkit.Mvvm, FluentValidation, BCrypt, ClosedXML, CsvHelper, QuestPDF, OxyPlot, MahApps.Metro.
- Arquitectura: Domain, Application, Infrastructure, Presentation.
- Entrada DI: `src/Presentation/App.xaml.cs`, metodo `ConfigurarServicios`.
- Ventana principal y menu: `src/Presentation/MainWindow.xaml` + `src/Presentation/ViewModels/MainViewModel.cs`.
- Servicio de sesion por inactividad: `src/Presentation/Services/ServicioInactividad.cs`.
- DataTemplates VM -> View: `src/Presentation/MainWindow.xaml`.
- Archivo de configuracion local: `src/Presentation/appsettings.Local.json`.

## Comandos

```powershell
# Build normal
dotnet build .\src\Presentation\SistemaAranceles.Presentation.csproj

# Tests
dotnet test

# Ejecutar app
dotnet run --project .\src\Presentation\SistemaAranceles.Presentation.csproj

# Buscar rapido
rg -n "texto" src
```

Si la app WPF esta abierta, el build puede fallar con `MSB3027`/`MSB3021` porque `SistemaAranceles.Presentation.exe` esta bloqueado. Preferir cerrar la app. Si el usuario quiere mantenerla abierta, validar con salida temporal:

```powershell
$out = Join-Path $env:TEMP 'SistemaArancelesCodexBuild\Presentation\'
dotnet build .\src\Presentation\SistemaAranceles.Presentation.csproj -p:OutDir=$out

$testOut = Join-Path $env:TEMP 'SistemaArancelesCodexTest\'
dotnet test -p:OutDir=$testOut
```

Ultima validacion conocida:

- Build con salida temporal: 0 errores.
- `dotnet test -p:OutDir=<temp>`: 72 pruebas superadas, 0 errores.

## Arquitectura y Convenciones

- Entidades: `src/Domain/Entities`.
- Enums y constantes: `src/Domain/Enums`, `src/Domain/Constantes`.
- DTOs: `src/Application/DTOs`.
- Casos de uso: `src/Application/UseCases`.
- Interfaces: `src/Application/Interfaces`.
- EF, repositorios y configuraciones: `src/Infrastructure/Persistence`.
- Vistas WPF: `src/Presentation/Views`.
- ViewModels WPF: `src/Presentation/ViewModels`.
- Estado de sesion y cache UI: `src/Presentation/State`.

Patrones:

- Interfaces `IRepositorio*` / `IServicio*`.
- Use cases con metodo principal `EjecutarAsync`.
- Commands y queries terminan en `*Command` o `*Query`.
- DTOs con propiedades `Display` para texto formateado.
- Repositorios concretos en Infrastructure.
- Scripts SQL idempotentes en `sql/`.

## WPF: Gotchas Importantes

### DataTemplates

Cada ViewModel que se asigna a `PaginaActual` necesita un `DataTemplate` en `MainWindow.xaml`.

La View debe tener constructor sin parametros; WPF la instancia desde XAML y el `DataContext` lo asigna el `ContentControl`.

### Menu Principal

El menu lateral se construye en `MainViewModel.ConstruirMenu()`.

Estado actual:

- `ItemMenu` hereda de `ObservableObject`.
- Cada item tiene `EstaSeleccionado`.
- `SeleccionarMenu(titulo)` desmarca todos y marca el item activo.
- `MainWindow.xaml` pinta el item activo con fondo resaltado, barra amarilla y sombra.
- Los textos largos usan `TextBlock` con `TextWrapping="Wrap"`, `TextTrimming="CharacterEllipsis"`, `MaxWidth="166"` y tooltip.

Cuando se agregue un modulo nuevo:

1. Agregar item en `ConstruirMenu`.
2. Agregar metodo de navegacion.
3. Llamar `SeleccionarMenu("Titulo exacto")` al navegar.
4. Registrar `DataTemplate` si `PaginaActual` apunta a un VM nuevo.

### Timer de Sesion

Archivos:

- `ServicioInactividad.cs`.
- `App.xaml.cs`.
- `MainViewModel.cs`.
- `MainWindow.xaml`.

Reglas actuales:

- El timeout viene de `Session:TimeoutMinutes`.
- `ServicioInactividad.TiempoRestante` expone el restante actual.
- `TiempoRestanteActualizado` se emite en tick, inicio y reset de actividad.
- `MainViewModel.TiempoRestanteSesion` se inicializa desde `TiempoRestante`.
- Al recuperar foco, `App.xaml.cs` ignora eventos de actividad durante 2 segundos para evitar que el timer salte a `44:59` solo por cambiar de ventana.

No volver al patron de "ignorar solo el primer input tras activacion"; se observo inestable por eventos multiples de foco/entrada.

### Combos en Cascada

Cuando cambia `Carrera` y se recargan `Escenarios`, limpiar seleccion antes de reemplazar la coleccion y usar bandera de supresion:

```csharp
_suprimirRecarga = true;
try
{
    EscenarioSeleccionado = null;
    Escenarios = new ObservableCollection<EscenarioProyeccion>(lista);
    EscenarioSeleccionado = Escenarios.FirstOrDefault();
}
finally
{
    _suprimirRecarga = false;
}
```

Las `partial void OnXChanged` deben salir si `_suprimirRecarga` o `EstaCargando` esta activo.

### `IsEnabled` y Converters

`IsEnabled` espera `bool`. No usar converters de `Visibility` en `IsEnabled`.

Usar propiedades booleanas en el VM, por ejemplo:

- `FormEsModoManual`
- `FormPorcentajeMatriculaEditable`
- `NoEstaGuardando`
- `RatioFormMesesEditable`
- `RatioFormAdicionalFijoEditable`

### Decimales

Para inputs monetarios usar parser flexible existente en ViewModels como `TryDecimalFlexible` o `TryDecimal`. Evitar `Replace(",", ".")` simple porque falla con separadores de miles.

### DataGrid y Scroll

Usar dimensiones estables y estilos existentes:

```xml
HeadersVisibility="Column"
GridLinesVisibility="Horizontal"
AlternatingRowBackground="#F8F9FF"
```

Si un `DataGrid` dentro de `ScrollViewer` captura la rueda del mouse, revisar el patron `DataGrid_PreviewMouseWheel` en `MantenimientoView.xaml.cs`.

## Modulos y Estado Funcional

### Seguridad, Usuarios y Auditoria

- Login con BCrypt.
- Sesion por inactividad.
- RBAC granular.
- Usuarios, roles/permisos y auditoria.

Permisos frecuentes:

- `US.{CREAR,EDITAR,ELIMINAR,VER}`
- `INF.{VER,EDITAR}`
- `AUD.VER`
- `AF.VER`
- `RD.VER`
- `MI.{VER,CREAR,EDITAR,ELIMINAR}`
- `DI_NG.{VER,EDITAR,CALCULAR}`

### Inflacion

- CRUD/importacion/proyeccion de inflacion anual.
- Inflacion importada tiene prioridad sobre estimada cuando coinciden.

### Tasa de Retencion y Estudiantes

- Tasa Retencion: configuracion por carrera/escenario, simulaciones y metas.
- Estudiantes: proyeccion por periodos, horas, consumo y docentes.
- Overrides de horas: `sql/KAN_OverrideHorasPeriodo.sql`.

Regla actual de docentes:

- `fila15` conserva horas acumuladas reales.
- Antes del desglose se aplica `decimal.Round(fila15[p], 0, MidpointRounding.AwayFromZero)`.
- `DesglosarDocentesPorPeriodo` normaliza a entero al inicio.
- UI usa `Periodos[p]` con enteros para personas y horas asignadas.
- No reintroducir `ValoresDisplay` decimal para horas docentes.

### Sueldos Carrera

- Vista: `src/Presentation/Views/CargosFacultad/CargosFacultadView.xaml`.
- VM: `src/Presentation/ViewModels/CargosFacultad/CargosFacultadViewModel.cs`.
- Query por periodo: `GenerarTablaSueldosPeriodoQuery`.
- Resumen pivot: `GenerarResumenSueldosQuery`.
- Tiempo parcial soporta `TipoContrato` / `TarifaHora` por KAN20b.

### Recursos y Depreciacion

- Vista/VM: `ActivosFijosView`, `ActivosFijosViewModel`.
- CRUD de activos, catalogo base, categorias, inversion futura, matrices de inversion y depreciacion.
- Scripts KAN24/KAN25.

### Mantenimiento e Inversion

Contenedor: `src/Presentation/Views/MantenimientoInversion/MantenimientoInversionView.xaml`.

Tabs:

- Mantenimiento.
- Activos Diferidos.
- Inversion Inicial.

Mantenimiento usa `sql/KAN28_servicios_mantenimiento.sql`.
Activos Diferidos usa `sql/KAN31_activos_diferidos.sql`.
Inversion Inicial integra activos diferidos, activos fijos, capital de trabajo e imprevistos.

### Capital de Trabajo

- View/VM: `CapitalTrabajoView`, `CapitalTrabajoViewModel`.
- Query principal: `ObtenerCapitalTrabajoPorCarreraQuery`.
- Script: `sql/KAN29_capital_trabajo.sql`.
- No usar `CarreraId = 1` ni `carreraId ?? 1`.
- A usa Sueldos Carrera del primer periodo.
- B/C/D son CRUD por carrera.
- E es resumen con cantidad, concepto, valor unitario, valor mensual y totales.

## Epica 9: Demanda e Ingresos

Vista: `src/Presentation/Views/DemandaIngresos/DemandaIngresosView.xaml`.
VM: `src/Presentation/ViewModels/DemandaIngresos/DemandaIngresosViewModel.cs`.

Tabs actuales:

1. Configuracion de Arancel.
2. Demanda Proyectada.
3. Presupuestos y Seguro.
4. Ingresos Proyectados.
5. Materiales en Cantidades.
6. Materiales Monetarios.

No existe pestana `7. Resumen`. El `Resumen consolidado` vive dentro de `1. Configuracion de Arancel`.

### KAN-32 Configuracion de Arancel

- Tabla: `configuracion_arancel_carrera`.
- Configuracion especifica por carrera + escenario tiene prioridad.
- Fallback: configuracion global de carrera (`escenario_proyeccion_id IS NULL`).
- Si no hay ninguna, mostrar advertencia.
- Modo Manual implementado.
- Modo Automatico pendiente de Epica 10; no inventar calculo.
- Cambio de escenario debe refrescar arancel efectivo sin usar cache viejo.

Archivos:

- `ObtenerArancelEfectivoQuery.cs`
- `GuardarConfiguracionArancelCarreraCommand.cs`
- `RepositorioConfiguracionArancelCarrera.cs`
- `sql/KAN32_configuracion_arancel_carrera.sql`

### KAN-33 Ingresos Proyectados

- Usa Demanda Proyectada + Arancel Efectivo.
- Calcula bruto, becas, neto y total general.
- Matriz Ciclo x Periodo.

Archivo:

- `CalcularIngresosProyectadosQuery.cs`

### KAN-34 Materiales con Inflacion

Tabla: `ratio_material_demanda`.

Unidades:

- `por_estudiante`: estudiantes \* consumo.
- `por_estudiante_mes`: estudiantes _ consumo _ meses_operativos.
- `fijo_periodo`: consumo.
- `por_docente`: docentes_necesarios \* consumo + cantidad_fija_adicional.

Notas criticas:

- `fijo_periodo` no multiplica por estudiantes ni meses.
- `por_docente` usa la fila `Docentes Requeridos` de Demanda Proyectada.
- `cantidad_fija_adicional` es decimal `NUMERIC(18,4) NOT NULL DEFAULT 0`.
- Si la unidad no es `por_docente`, se guarda adicional fijo en 0.
- Materiales Monetarios no recalcula reglas: consume cantidades y aplica `precio * inflacion`.
- Precio unitario viene de Capital de Trabajo / item vinculado.
- No tocar SQL salvo constraint/columna necesaria y siempre como script idempotente.

Scripts:

- `sql/KAN34_ratio_material_demanda.sql`
- `sql/KAN34_update_ratio_material_demanda_unidad_fijo_periodo.sql`
- `sql/KAN34_add_por_docente_ratio_material_demanda.sql`

Archivos:

- `RatioMaterialDemanda.cs`
- `UnidadRatioMaterial.cs`
- `RatioMaterialDemandaDto.cs`
- `GuardarRatioMaterialDemandaCommand.cs`
- `CalcularMaterialesPorPeriodoQuery.cs`
- `RepositorioRatioMaterialDemanda.cs`
- `DemandaIngresosViewModel.cs`
- `DemandaIngresosView.xaml`

### KAN-35 Presupuestos y Seguro

- Amplia Datos Institucionales.
- Presupuestos base institucionales.
- Montos asignados a carrera.
- Seguro, becas y valores por estudiante.
- Inflacion aplicada.

Script:

- `sql/KAN35_datos_institucionales_demanda_ingresos.sql`

## SQL / Supabase

Reglas:

- Scripts en `sql/`.
- Preferir `ADD COLUMN IF NOT EXISTS` y `DROP CONSTRAINT IF EXISTS`.
- No borrar ni migrar datos existentes salvo instruccion explicita.
- No ejecutar Supabase desde codigo.
- Si el usuario reporta error de constraint `23514`, revisar CHECK constraints y preparar SQL idempotente.

Scripts recientes importantes:

- `KAN32_configuracion_arancel_carrera.sql`
- `KAN34_ratio_material_demanda.sql`
- `KAN34_update_ratio_material_demanda_unidad_fijo_periodo.sql`
- `KAN34_add_por_docente_ratio_material_demanda.sql`
- `KAN35_datos_institucionales_demanda_ingresos.sql`

## Tests

Proyectos:

- `tests/Application.Tests`
- `tests/Presentation.Tests`

Areas cubiertas:

- Proyeccion estudiantes.
- Calculo docentes.
- Inflacion/sueldos.
- Recursos y depreciacion.
- Inversion futura.
- Datos institucionales.
- Demanda e Ingresos: aranceles y materiales.
- ViewModel de consumos de estudiantes.

Cuando cambien reglas de materiales o arancel, revisar `tests/Application.Tests/DemandaIngresos/MaterialesYArancelTests.cs`.

## Pendientes Conocidos

- Modo Automatico de arancel depende de Epica 10/Costo Carrera.
- Costos/Gastos, Financiamiento, Balance, Reportes y Analisis Financiero siguen parciales o pendientes.
- Sueldos Planta Central existe en estructura y tests parciales; revisar alcance antes de modificar.
- README y este archivo deben actualizarse cuando cambien Demanda e Ingresos, docentes, menu o sesion.

## Checklist Antes de Finalizar Cambios

1. `git status --short`.
2. `rg` de referencias afectadas.
3. Build de Presentation.
4. `dotnet test` si hay cambios de logica o contratos.
5. `git diff --check`.
6. Resumen final con archivos tocados y validacion ejecutada.
